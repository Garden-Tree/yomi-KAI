services:
  yomi-kai:
    build: .
    container_name: yomi-kai
    restart: unless-stopped
    volumes:
      # 設定ファイル
      - ./config.ini:/app/config.ini:ro
      # 辞書データとログを永続化
      - ./dict:/app/dict
      - ./log:/app/log
    # Google TTSを使用する場合は、config.iniにAPIキーを設定し、USE_GOOGLE_TTS=Trueに変更してください。
